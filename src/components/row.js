import React, { Component } from 'react';


class Row extends Component {

    //have rows dictate when the obstacles are created 
    //have the row move 
    //have the row go away after an amount of time and then add it to the back
    //create the rows in the begining of the game 
    //have the row be the thing the moves 
    //it'll be an array of arrays
    //what will state have?
    //all we need to change is the y
    //hope for the best
    render(){
        return(
            <div>

            </div>
        )
    }

}

export default Row;